"use client"
import { useEffect, useState } from "react"
import { API_URL } from "../../../lib/config"
async function api(path,opt={}){const res=await fetch(`${API_URL}${path}`,{credentials:"include",headers:{"Content-Type":"application/json",...(opt.headers||{})},...opt}); const j=await res.json().catch(()=>({})); if(!res.ok) throw new Error(j.message||"Failed"); return j}
const empty={name:"",email:"",phone:"",username:"",password:""}
export default function AdminPromoters(){const [items,setItems]=useState([]),[form,setForm]=useState(empty),[msg,setMsg]=useState(""); async function load(){setItems(await api('/api/admin/influencers'))} useEffect(()=>{load().catch(e=>setMsg(e.message))},[]); function set(k,v){setForm(p=>({...p,[k]:v}))} async function create(){try{await api('/api/admin/influencers',{method:'POST',body:JSON.stringify(form)}); setForm(empty); setMsg('Promoter account created'); await load()}catch(e){setMsg(e.message)}} async function toggle(x){try{await api(`/api/admin/influencers/${x.id}/status`,{method:'PATCH',body:JSON.stringify({isActive:!x.is_active})}); await load()}catch(e){setMsg(e.message)}} return <main><h1 className="text-3xl font-black">Promoters</h1><p className="mt-2 text-slate-500">这里创建 / approve Promoter 账号。Promoter 可以是网红、marketer、agency 或流量手，登录后看产品、复制专属 link、看佣金。</p><section className="mt-6 rounded-3xl bg-white p-6 shadow"><h2 className="text-xl font-black">新增 Promoter 账号</h2><div className="mt-4 grid gap-3 md:grid-cols-5"><input className="rounded-2xl border p-3" placeholder="名字" value={form.name} onChange={e=>set('name',e.target.value)}/><input className="rounded-2xl border p-3" placeholder="Email" value={form.email} onChange={e=>set('email',e.target.value)}/><input className="rounded-2xl border p-3" placeholder="Phone" value={form.phone} onChange={e=>set('phone',e.target.value)}/><input className="rounded-2xl border p-3" placeholder="Username/ref，例如 jack" value={form.username} onChange={e=>set('username',e.target.value)}/><input className="rounded-2xl border p-3" placeholder="Password" value={form.password} onChange={e=>set('password',e.target.value)}/></div><button onClick={create} className="mt-4 rounded-full bg-slate-950 px-6 py-3 font-black text-white">创建 Promoter</button>{msg?<p className="mt-4 rounded-2xl bg-blue-50 p-3 font-bold text-blue-700">{msg}</p>:null}</section><div className="mt-6 grid gap-4">{items.map(x=><div key={x.id} className="rounded-3xl bg-white p-5 shadow"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><b>{x.name}</b><p className="text-sm text-slate-500">{x.email} · @{x.username} · ref={x.ref_code}</p></div><button onClick={()=>toggle(x)} className={`rounded-full px-5 py-2 font-black ${x.is_active?'bg-red-50 text-red-700':'bg-green-50 text-green-700'}`}>{x.is_active?'Disable':'Enable'}</button></div></div>)}</div></main>}
