"use client"
import { useEffect, useState } from "react"
import Link from "next/link"
import { API_URL } from "../../../lib/config"
async function iapi(path){const res=await fetch(`${API_URL}${path}`,{credentials:"include"}); const j=await res.json().catch(()=>({})); if(!res.ok) throw new Error(j.message||"Failed"); return j}
function money(v){return `RM ${Number(v||0).toFixed(2)}`}
export default function PromoterOrders(){const [orders,setOrders]=useState([]),[err,setErr]=useState(""); useEffect(()=>{iapi('/api/promoter/orders').then(setOrders).catch(e=>setErr(e.message))},[]); if(err)return <main className="p-10"><Link href="/promoter/login" className="rounded-full bg-slate-950 px-6 py-3 font-black text-white">Promoter Login</Link></main>; return <main className="min-h-screen bg-[#f7f4ee] px-4 py-10"><div className="mx-auto max-w-6xl"><Link href="/promoter" className="font-black text-blue-700">← 返回 Promoter Dashboard</Link><h1 className="mt-5 text-4xl font-black">我的佣金订单</h1><div className="mt-6 overflow-hidden rounded-[28px] bg-white shadow"><table className="w-full text-left text-sm"><thead className="bg-slate-100"><tr><th className="p-4">产品</th><th className="p-4">金额</th><th className="p-4">佣金</th><th className="p-4">状态</th><th className="p-4">日期</th></tr></thead><tbody>{orders.map(o=><tr key={o.id} className="border-t"><td className="p-4 font-bold">{o.product_name}</td><td className="p-4">{money(o.total_amount)}</td><td className="p-4 font-black text-amber-700">{Number(o.commission_rate||0)}% · {money(o.commission_amount)}</td><td className="p-4">{o.commission_status||'-'}</td><td className="p-4">{new Date(o.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></div></main>}
